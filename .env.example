# Database Connection (PostgreSQL / Supabase / Vercel Postgres)
DATABASE_URL="postgresql://postgres:password@localhost:5432/jeli_db?schema=public"

# AI Key Configuration (Protected on server side, never exposed to client)
GEMINI_API_KEY="your_gemini_api_key_here"

# Next.js Server Config
PORT=3000
NEXT_PUBLIC_APP_URL="http://localhost:3000"
